# --- Minimalist Prompt ---
# %1~ shows only the current directory name.
# %# shows % for normal users, # for root.
PROMPT='%1~ $ '

# Optional: Add some basic aliases for a cleaner workflow
alias ll='ls -lh'
alias c='clear'
